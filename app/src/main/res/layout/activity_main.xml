<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/sahife"
    tools:context="ir.quantumsoft.al_sahifaal_sajjadiyya.MainActivity">

    <ImageView
        android:layout_height="550dp"
        app:srcCompat="@drawable/logobig"
        android:id="@+id/imageView"
        android:layout_width="450dp"
        android:layout_above="@+id/arabicBtn"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="25dp"
        android:clickable="true"
        android:onClick="getAbout" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ورود "
        style="@style/Widget.AppCompat.Button.Borderless.Colored"
        android:layout_marginBottom="68dp"
        android:textSize="22sp"
        android:theme="@style/MyButton"
        android:id="@+id/arabicBtn"
        android:typeface="normal"
        android:textStyle="normal|bold"
        android:onClick="getArabic"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>
